package core.day1.constructors;

// The constructor is the first thing which runs when an object is created of that class.
// It allows us to define HOW we want an object to be created either by default
// or when we pass in certain arguments to the constructor.

public class MultipleConstructorExample {

	int num1;
	int num2;
	int num3;

	public MultipleConstructorExample(int num1) {
		// we can set the value of the num1 variable if only 1 number is passed
		// as an argument when this constructor is called.
	}

	public MultipleConstructorExample(int num1, int num2) {
		// we can set the value of num1 & num2 if 2 numbers are passed
		//as an argument when this constructor is called..

	}

}
